class LinkMetadata {
  final String title;
  final String size;
  final String mimeType; // Make sure this is declared

  LinkMetadata({
    required this.title,
    required this.size,
    required this.mimeType,
  });
}
